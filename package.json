{
  "name": "cloudinary-mongodb-image",
  "version": "1.0.0",
  "description": "Small web application to upload images to Cloudinary and store image URLs in MongoDB",
  "main": "index.js",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm install --prefix frontend && npm install --prefix backend && npm run build --prefix frontend && cpx \"frontend/build/**/*\" backend/build",
    "start": "npm run start --prefix backend"
  },
  "devDependencies": {
    "cpx": "^1.5.0"
  },
  "engines": {
  "node": "18.x"
},
  "keywords": [],
  "author": "Susana",
  "license": "ISC"
}
